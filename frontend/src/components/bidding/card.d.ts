import type { ReactNode } from "react";
export default function Card({ children, className, showButton, }: {
    children: ReactNode;
    className?: string;
    showButton?: boolean;
}): import("react/jsx-runtime").JSX.Element;
